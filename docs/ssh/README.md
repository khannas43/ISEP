# SSH keys (local use only)

Store SSH key files here for deployment (e.g. `id_rsa` or `*.pem`).

**Private keys in this folder are ignored by git** (see `.gitignore`). Do not commit them to the repository.
